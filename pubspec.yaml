name: login_app
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  firebase_core: ^4.11.0
  firebase_auth: ^6.5.4
  provider: ^6.1.5+1
  cloud_firestore: ^6.6.0
  # Interop del DOM para el ticket de venta. Reemplaza a `dart:html`, que no
  # existe en la compilación `--wasm` con la que se publica el sitio.
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  # Los assets fuente viven en `res/`, NO en `assets/`, y es a propósito:
  # el workflow de GitHub Pages publica el build compilado en la raíz del
  # repo, y ahí `assets/` es el directorio de SALIDA (hace `rm -rf assets`
  # y copia encima `build/web/assets`). Flutter anida los assets de la app
  # un nivel más adentro en esa salida (`assets/res/images/…`), así que un
  # archivo fuente en `assets/` lo borra el deploy y el `flutter analyze`
  # de la siguiente corrida falla con `asset_does_not_exist`.
  # Cualquier asset nuevo va aquí abajo, nunca en `assets/`.
  assets:
    - res/images/AuraVitae-Login-backgroundImg.webp
    - res/images/aura-vitae-logo.png
    - res/images/aura-vitae-logo-navbar.png
    # Fotos del catálogo. Se sirven como archivos sueltos y se descargan solo
    # cuando una pantalla las pide, así que no entran en el arranque de la app
    # ni afectan el tiempo de carga inicial.
    - res/images/products/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # Tipografía del design system — Montserrat (marca) + Inter (cuerpo).
  #
  # Van empaquetadas, no enlazadas desde Google Fonts. El `<link>` que
  # había en `index.html` no servía de nada: el renderer pinta el texto
  # sobre un canvas, así que ningún elemento del DOM usa esas familias y
  # el navegador nunca llegaba a descargar los archivos. El motor solo
  # encuentra las fuentes que se declaran aquí, de modo que cada
  # `fontFamily: 'Montserrat'` del código caía al Roboto por defecto.
  #
  # Se empaqueta el subconjunto latino en woff2 (~20 KiB por peso en vez
  # de ~200 KiB del TTF completo). Los glifos fuera de ese rango —el
  # emoji del saludo, la flecha de "Nombre A→Z"— los resuelve el
  # mecanismo de respaldo del motor.
  #
  # Solo los pesos que el código usa de verdad, auditados sobre
  # `app_theme.dart` y las pantallas: Montserrat 600/700/800 e Inter
  # 400/600/700. El peso 500 se pedía antes y no lo usa nadie.
  fonts:
    - family: Montserrat
      fonts:
        - asset: res/fonts/Montserrat-SemiBold.woff2
          weight: 600
        - asset: res/fonts/Montserrat-Bold.woff2
          weight: 700
        - asset: res/fonts/Montserrat-ExtraBold.woff2
          weight: 800
    - family: Inter
      fonts:
        - asset: res/fonts/Inter-Regular.woff2
          weight: 400
        - asset: res/fonts/Inter-SemiBold.woff2
          weight: 600
        - asset: res/fonts/Inter-Bold.woff2
          weight: 700
